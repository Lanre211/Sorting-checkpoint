// problem 1
// take in three cards namely card 1, card 2, card 3
// assign the cards to an array
// if a new card is taken in, and its not in the array 
// the new card should be placed after the last index of the array


let cardarr = ("1,2,3")

console.log(cardarr);

 i = cardarr.length [i+1]


//problem 2
// compare i to the remaining index 
// if i is greater than the next index
// swap i with the next index

function Newcard(insert) {
  For 
    if(cardarr[i] > cardarr[i+1]);
    
    THEN
    
    swap (i, cardarr.length[i+1]);
}
